# LG TV Controller — Environment Variables

# TV
TV_HOST=192.168.15.3
TV_PORT=3001
TV_MAC=AC:5A:F0:C4:DD:F2
WEB_PORT=8888

# Google OAuth2 (opcional — sem isso, acesso é livre)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://tv.runtask.work/auth/callback
ALLOWED_EMAILS=felipeosmar@gmail.com

# Session secret (32+ chars, gere com: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
SESSION_SECRET=
